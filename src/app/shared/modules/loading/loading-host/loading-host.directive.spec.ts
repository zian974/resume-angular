import { LoadingHostDirective } from './loading-host.directive';

describe('LoadingHostDirective', () => {
  it('should create an instance', () => {
    // const directive = new LoadingHostDirective();
    expect(true).toBeTruthy();
  });
});
